# Production lifecycle partial GREEN and regression blockers

## Goal and release outcome

Complete Release1/Slice4be under Architecture v4.11 and Plan022 without regressing
accepted workflows. Current work advances D18 comprehensive Production observations;
the full goal remains active and incomplete. Do not mark it complete or blocked:
independent lifecycle/Action Path work remains possible while capture is unavailable.

## Current verified state

Last verified: 2026-09-24,23:28 UTC. Code main: **d23cf6c**. Documentation main
implementation checkpoint: **6107fe6**, followed by this handoff/pointer commit.
Both implementation commits preserve the six lifecycle controls as partial acceptance.
Commit/push this handoff and verify both repositories synchronized before exiting.

Candidate: `deploy/validation-production-lifecycle`; frozen predecessor:
`deploy/validation-production-header`. Do not overwrite either. Five builds,
compiles and cold-start references pass. Compiled comparison:246 components,
exactly six changed and two added; Domain components unchanged. Excel is closed;
all tests have terminated. Close relevant Excel instances before rebuilding.

Code worktree should be clean after this checkpoint. Preserve unrelated docs:
modified `last handoff/067 Partial Goal Action Path NAS Contract.md` and untracked
`expert guidance docs/023 Slice 4be Critique.md`. Their pins remain verified in
`reports/runtime/evaluation-selection-unrelated-doc-pins.json`.

## Decisions and constraints

D18's lifecycle section was written before runtime implementation under the
approved discovered-control/semantic-inheritance rule. Controls1.249 and Plan022
are synchronized. Catalog13 adds six Process/Recipe Save Draft/Release/Obsolete
IDs, preserving1-12. Owner: PRODUCTION_DESIGN_LIFECYCLE. CONFIRMED is command
completion with Unknown Domain effect, never proof of application. Exact Designs
references come from the owning writer through typed local facts/primitive outputs.

The actual handlers reuse the captured-context/permission guard. Shared submission
helpers/imports do not manufacture clicks. Preserve immutable System_Key, unknown
columns, headless Core/Domain, Operations packaging and existing capability rules.
Designs SourceEventsApplied and new lifecycle CommandCompleted evaluation mappings
remain unimplemented: follow the now-explicit D18 rules, not message/status inference.

Plan022 records one specific static exception: literal-normalized ControlIds lists
in draft/lifecycle catalog modules add one duplicate group. Do not generalize it.

## Evidence and traceability

Primary record: code `tests/integration/plan022_slice4be_production_lifecycle_results.md`.
Final focused RED139 PASS/155 expected FAIL -> **GREEN294/294**, identical checks,
normal closure, restored settings, immutable packages and zero Application failures.
Protects all six actual successful handlers, exact source IDs, metadata/history,
denial/rejection, unknown columns and stale target/session guards.

Retained regressions: **Settings202/202**, smoke**86/86**, earlier draft/recording/
publication/diagnostic **390/390**, exact preceding identities. Settings and draft
regression close normally; draft exit is delayed. The residual Excel process exited
before the planned recovery command, so **no termination occurred for390**.
Smoke's existing helper may terminate Excel; do not call it unassisted closure.
No native captures were requested/accepted. Layout8/8+7/7; bulk-import source8/8.

Final static:253 components/6058 procedures/133154 lines;9 literal/45 unresolved
dynamic calls;192 duplicate groups with the one reviewed exception; all28 oversized
limits hold (form11743, writer3020). Three schemas/280 PowerShell parses pass.

Private receipts under reports/runtime:
- `production-lifecycle-focused-verification.json`
- `production-lifecycle-final-static-verification.json`
- `production-lifecycle-passing-regressions-verification.json`
- `production-lifecycle-designer-verification.json`
- `production-lifecycle-chain-failures-verification.json`

Two candidate chains and the frozen predecessor all fail at inventory projection
rebuild, `modProcessor.RunBatchReportForAutomation`, RPC800706BE: chain5/1,
live-role32/1, Create Warehouse15/15. Recovery Excel required verified termination;
original controllers restored settings and three reports. A/B comparison establishes
that the failure also occurs on the predecessor, **not its cause**. Full-chain
acceptance remains open. Reusable Production separately stops at
`mProduction.RunProductionBatchScaleContractTest`, same HRESULT, before its67
Boolean observations; its existing helper restores settings and closes Excel.

Cursor access returns error5 at23:07:30 and23:17:43 UTC. Possible Group Policy
involvement remains unproven; no policy/permission edit was made. The user was asked
whether both host and RDP-client desktops are unlocked; no answer received yet.

## Do Not Repeat

- Do not treat lifecycle294 or earlier draft390 as new lifecycle Action Path acceptance.
- Do not relabel either failed chain or reusable run as GREEN because the predecessor fails.
- Do not attach another COM client to investigate a residual Excel process after Quit.
- GetRecipeGraph is the real Recipe query; GetRecipeVersion does not exist.
- The intermediate closed-workbook lifecycle fixture hit VBA80010007 during Stage;
  its assisted run and malformed reference-case arrays are not meaningful RED.
  Current lifecycle gate omits that case explicitly; earlier draft closed-book guards pass.
- Preserve the legacy Slice4x report. Its old caption/Viewer text predicates fail on
  prior source too; this does not replace current packaged evidence. Bulk-import's
  source assertion now follows SubmitDesignerAction(True, PROCESS_SAVE).

## Assumptions to re-verify

Excel closed, candidate/predecessor hashes, desktop access and repository synchronization
are time-dependent. Never infer normal exit from a passing behavioral JSON alone.
Current optional capture question is not authorization to assume an unlocked desktop.

## Open questions and blockers

New lifecycle family still needs pending/uncertain/error owner facts, native cancellation,
tracking-off/failure, re-entrancy and a corrected closed-workbook fixture; then original
recording/publication and both Action Path methods with exact Designs applied/awaiting/
incomplete evidence. Full-chain/reusable RPC and native capture remain unaccepted.
Production census:68 constructed buttons,13 registered IDs,55 pending;14 legacy and34
non-button handlers remain separately classified. Broader guide transfer/comparison,
carrier D5 conflict, other Operations/Admin coverage and human/NAS acceptance remain
in the current remaining-acceptance checklist. Preserve that full release scope.

## Immediate next action

Add a packaged actual-handler test for confirmed Designs submission followed by a
controlled owner-processing failure, establish its PENDING/exact-reference RED on the
frozen predecessor, then verify the lifecycle candidate before further runtime edits.

## Critical references

Architecture D18 lifecycle section; Plan022 active lifecycle paragraph/exception;
controls1.249; `invSys-Production-Tracking-Coverage-v1.md`; code remaining checklist;
`Test-Slice4beProductionLifecycle.ps1`, `Slice4beProductionLifecycle.ps1`;
`frmProduction.DesignerDraftAction/ExecuteDesignerLifecycle/SubmitDesignerAction`;
`modProductionReusableDesigns.SubmitReusableDesignEvent`, `cProductionLifecycleFacts`;
`modRoleEventWriter.QueueDesignEventCurrent`, `modProductionLifecycleCodes`,
`modActivityReferences`, `modEvaluationMatches`, `modEvaluationSources`.
