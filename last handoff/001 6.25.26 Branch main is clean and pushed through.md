Here is the next-session handoff.

**Current State**
Branch `main` is clean and pushed through:

`6054cd5 Repair shipment row commits without mutating NAS inventory`

Verified locally:
- Build passed.
- Phase 6 shipping slice `111-153`: `43/43` passed.
- Tests now cover Add/Remove/Update missing local `invSys` row repair and NAS `TOTAL INV` protection.

But live testing still failed after that:
- Add to an existing same `Ref` + same item row: `invSys ROW 89 not found`
- Update Row qty: `invSys ROW 89 not found`
- Remove row: reported same error, but timing log said `CommitCurrentLine UPDATE start`, so first check whether Remove is actually calling UPDATE or whether the status box retained old timing text.

**Most Important First Check**
Confirm the test machine is actually running the rebuilt/pushed Shipping add-in from commit `6054cd5`. The local tests pass exactly the missing-row scenarios, so either:
- the live Excel session still has an older `invSys.Shipping.xlam` loaded, or
- the live form path differs from the unit seam, likely selected-row/listbox state or operator workbook resolution.

**What We Fixed Today**
- Add/Update/Remove now try to repair missing local `invSys` rows before reserve/release math.
- Failed Add/Update row edits roll back instead of leaving duplicate or partial rows.
- Existing `TOTAL INV` no longer gets overwritten from Projected/local display during reservation qty changes.
- Added tests for:
  - Add repairs missing `invSys` row from visible NAS.
  - Remove repairs missing `invSys` row before release.
  - Update repairs missing `invSys` row before delta.
  - Existing merge behavior still results in one row.
- Added a test trace seam because VBA ByRef report was hiding failure text.

**Still Not Done From `011 deep architectural audit, response to 010.md`**
No, we did not finish all of it.

Done or partly done:
- Instrumentation/timing exists and helped isolate slow paths.
- Form load performance was improved from ~60 seconds to ~2-4 seconds user-perceived.
- NAS boundary was tightened in several shipment commit paths.
- Previous-session active rows were cleared on initial open.
- History export exists.

Still open:
- `GetWritableShippingInvSysTable` has not been fully split into read/display vs mutable staging paths.
- `ReconcileShippableTotalsFromVersionInventory` has not been fully quarantined away from all Shipments paths.
- We have not fully audited every remaining `TOTAL INV` writer behind a guard wrapper.
- Live Add/Update/Remove still fails despite local tests, so the form path and deployed add-in state need reconciling.
- History readability and Clear History button remain open.
- A proper live-form seam test that exercises the actual selected listbox/form path is still needed.

**Recommended First Steps Next Session**
1. Reload/reinstall the current `deploy/current/invSys.Shipping.xlam` on the test machine and confirm the loaded add-in build is from `6054cd5`.
2. In `frmShipmentsTally`, verify `mBtnRemove_Click` calls `CommitCurrentLine "DELETE"` and that the clicked control is the expected Remove button.
3. Add a temporary top-message diagnostic inside live `CommitCurrentLine` that prints:
   - `actionName`
   - selected `rowIndex`
   - `rowValue`
   - `displayedNasQty`
   - `displayedAvailableQty`
   - whether `mShippables` is non-empty
   - target workbook name
4. Reproduce Add-to-existing, Update, Remove once each.
5. If still failing, compare live inputs against passing tests `TestShippingAdd_RepairsMissingInvSysRowFromVisibleNas`, `TestShippingUpdate_RepairsMissingInvSysRowBeforeDelta`, and `TestShippingRemove_RepairsMissingInvSysRowBeforeRelease`.

The next session should not start with broad refactor. First prove whether live Excel is running current code and whether Remove is actually dispatching `DELETE`.